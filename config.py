import os

BASE_DIR = os.path.dirname(__file__)

SETTINGS_FILE = os.path.join(
    BASE_DIR,
    "settings.json"
)

ALARM_FOLDER = os.path.join(
    BASE_DIR,
    "alarms"
)

ASSET_FOLDER = os.path.join(
    BASE_DIR,
    "assets"
)